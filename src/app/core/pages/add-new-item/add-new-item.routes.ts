import { Routes } from "@angular/router";
import { AddNewItemPage } from "./add-new-item.page";

export const NEWITEM_ROUTES: Routes = [
    {
        path: '',
        component: AddNewItemPage
    }
]